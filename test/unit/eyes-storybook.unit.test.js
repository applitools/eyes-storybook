'use strict'
const {describe, it} = require('mocha')
const {expect} = require('chai')

describe('eyes-storybook', function() {
  it('should be amazing', async () => {
    expect(4).to.equal(4)
  })
})
